#include<stdio.h>
#include<string.h>
#include<stdlib.h>
main(){
    FILE *fp,*out,*Sym_ID,*Sym_KEY,*Sym_CON,*Sym_OP,*Sym_ERROR;
    char ch;
    char string[100];
    int id=0,constant=0,key=0,op=0,check,n=0,alpha_check=0;
    fp=fopen("input.txt","r");
    out=fopen("output.txt","w");
    Sym_ID=fopen("symbol_id.txt","w");
    Sym_KEY=fopen("symbol_key.txt","w");
    Sym_CON=fopen("symbol_con.txt","w");
    Sym_OP=fopen("symbol_op.txt","w");
    Sym_ERROR=fopen("error.txt","w");
    ch=fgetc(fp);
    while(ch!=EOF)
    {
       if(isalpha(ch) || ch=='#')
        {
  	   char string[100]="";
           if(ch=='#')
            {
		         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
             if(ch=='i')
              {
		         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                if(ch=='n')
                 {
			 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                   if(ch=='c')
                   {
			 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                    if(ch=='l')
                    {
			           string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                      if(ch=='u')
                       {
                          string[n++]=ch;
                          ch=fgetc(fp);
                          check=(int)ch;
                      if(ch=='d')
                      {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                       if(ch=='e')
                       {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          {
                             fprintf(out,"<KEYWORD,%d>",key);
                             fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                             key=key+1;
                          }
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"%s\n",string);
                               fprintf(Sym_ERROR,"%s\n",string);
                           }
                       }
                       else
                        {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) ||(check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"%s\n",string);
                                fprintf(Sym_ERROR,"%s\n",string);
                         }
                      }
                      else
                        {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) ||(check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"%s\n",string);
                                fprintf(Sym_ERROR,"%s\n",string);
                         }
                       }
                     else
                      {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"%s\n",string);
                                fprintf(Sym_ERROR,"%s\n",string);
                      }
				    }
                    else
                        {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) ||(check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"%s\n",string);
                                fprintf(Sym_ERROR,"%s\n",string);
                        }
                   }
                   else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"%s\n",string);
                                fprintf(Sym_ERROR,"%s\n",string);

                           }
                 }
                 else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"%s\n",string);
                                fprintf(Sym_ERROR,"%s\n",string);

                           }

              }
              else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"%s\n",string);
                               fprintf(Sym_ERROR,"%s\n",string);
                           }
                  n=0;
            }
           else if(ch=='a')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='u')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='t')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='o')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                                    	fprintf(out,"<KEYWORD,%d>",key);
					fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                                fprintf(out,"<ID,%d>",id);
                                fprintf(Sym_ID,"<%s,%d>\n",string,id);
                                id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
                               fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
                               fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
                               fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
           else if(ch=='b')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='r')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='e')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='a')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='k')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                                        fprintf(out,"<KEYWORD,%d>",key);
                                        fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
                                        fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
           else if(ch=='c')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='a')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='s')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='e')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
				  else if(ch=='h')
				   {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='a')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='r')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
					 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='o')
                   {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
						if(ch=='n')
						{
							string[n++]=ch;
							ch=fgetc(fp);
							check=(int)ch;
							if(ch=='s')
							{
								string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='t')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
					fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else if(ch=='t')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='i')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='n')
                         	{
                         		    string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                                    if(ch=='u')
                                    {
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
										if(ch=='e')
										{
											if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
											{
												fprintf(out,"<KEYWORD,%d>",key);
												fprintf(Sym_KEY,"<%s,%d>\n",string,key);
												key=key+1;
											}
											else
											{
												while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
												{
													string[n++]=ch;
													ch=fgetc(fp);
													check=(int)ch;
												}
												fprintf(out,"<ID,%d>",id);
												fprintf(Sym_ID,"<%s,%d>\n",string,id);
												id++;
											}
										}
										else
											{
												while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
												{
													string[n++]=ch;
													ch=fgetc(fp);
													check=(int)ch;
												}
												fprintf(out,"<ID,%d>",id);
												fprintf(Sym_ID,"<%s,%d>\n",string,id);
												id++;
											}
								  }
								  else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
                          }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                         else
                         {
							   while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                    else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
				   }
                  else
                  {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
           else if(ch=='d')
            {
				string[n++]=ch;
                ch=fgetc(fp);
                check=(int)ch;
				if(ch=='e')
				{
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='f')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='a')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='u')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='l')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch=='t')
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
										if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
										{
											fprintf(out,"<KEYWORD,%d>",key);
											fprintf(Sym_KEY,"<%s,%d>\n",string,key);
											key=key+1;
										}
										else
										{
											while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
											{
												string[n++]=ch;
												ch=fgetc(fp);
												check=(int)ch;
											}
											fprintf(out,"<ID,%d>",id);
											fprintf(Sym_ID,"<%s,%d>\n",string,id);
											id++;
										}
								   }
								   else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
				else if(ch=='o')
				{
						string[n++]=ch;
						ch=fgetc(fp);
						check=(int)ch;
						if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
						{
							fprintf(out,"<KEYWORD,%d>",key);
							fprintf(Sym_KEY,"<%s,%d>\n",string,key);
							key=key+1;
						}
						else if(ch=='u')
						{
							string[n++]=ch;
							ch=fgetc(fp);
							check=(int)ch;
							if(ch=='b')
							{
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='l')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='e')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
						else
						{
							while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
							{
								string[n++]=ch;
								ch=fgetc(fp);
								check=(int)ch;
							}
							fprintf(out,"<ID,%d>",id);
							fprintf(Sym_ID,"<%s,%d>\n",string,id);
							id++;
					   }
				}
				else
				{
					while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
					{
						string[n++]=ch;
						ch=fgetc(fp);
						check=(int)ch;
					}
					fprintf(out,"<ID,%d>",id);
					fprintf(Sym_ID,"<%s,%d>\n",string,id);
					id++;
				}
				n=0;
		  }
		    else if(ch=='e')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='l')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='s')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='e')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='n')
                   {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='u')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='m')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='x')
                   {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='t')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='e')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='r')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='n')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else if(ch=='f')
			{
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='l')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='o')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='a')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='t')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='o')
                   {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='r')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else if(ch=='g')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='o')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='t')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='o')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else if(ch=='i')
            {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='n')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='t')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else if(ch=='f')
                          {
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                         n=0;
                    }
            else if(ch=='l')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='o')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='n')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='g')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else if(ch=='r')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='e')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='t')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='u')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='r')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch=='n')
									{
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								   }
								   else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else if(ch=='g')
                          {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
							if(ch=='i')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='s')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='t')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='e')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch=='r')
									 {
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								  }
								  else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}

								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }

                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else if(ch=='s')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='h')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='o')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='r')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='t')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='i')
                   {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='g')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='e')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='e')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='d')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='z')
                   {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='e')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='o')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='f')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
                  else if(ch=='t')
                   {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='a')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='t')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='i')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='c')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else if(ch=='r')
                    {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='u')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='c')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='t')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
                  else if(ch=='w')
                    {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='i')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='t')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='c')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='h')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }

                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else if(ch=='t')
           {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='y')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='p')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='e')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='d')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch=='e')
		                            {
										string[n++]=ch;
									    ch=fgetc(fp);
									    check=(int)ch;

									if(ch=='f')
									{
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								  }
								  else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								  }
								   else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
             else if(ch=='u')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='n')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='i')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='o')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='n')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else if(ch=='s')
                          {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='i')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='g')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='n')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='e')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch=='d')
									{
										string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								  }
								  else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }

                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
           else if(ch=='v')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='o')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='i')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='d')
                         	{
                         		string[n++]=ch;
                                	ch=fgetc(fp);
                                	check=(int)ch;
                         	  if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
                          	{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
                          	}
                          else
                           {
                              while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                           }
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else if(ch=='l')
                          {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='a')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='t')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='i')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='l')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch=='e')
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								  }
								  else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
            }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
             else if(ch=='w')
            {
                         string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                  if(ch=='h')
                    {
                    	 string[n++]=ch;
                         ch=fgetc(fp);
                         check=(int)ch;
                         if(ch=='i')
                         {
                         	string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                         	if(ch=='l')
                         	{
                         		string[n++]=ch;
                                ch=fgetc(fp);
                                check=(int)ch;
                                if(ch=='e')
                                {
									string[n++]=ch;
									ch=fgetc(fp);
									check=(int)ch;
									if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
									{
                            		 fprintf(out,"<KEYWORD,%d>",key);
									 fprintf(Sym_KEY,"<%s,%d>\n",string,key);
                            		 key=key+1;
									}
									else
									{
										while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
										{
											string[n++]=ch;
											ch=fgetc(fp);
											check=(int)ch;
										}
										fprintf(out,"<ID,%d>",id);
										fprintf(Sym_ID,"<%s,%d>\n",string,id);
										id++;
									}
								}
								else
								{
									while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
									{
										string[n++]=ch;
										ch=fgetc(fp);
										check=(int)ch;
									}
									fprintf(out,"<ID,%d>",id);
									fprintf(Sym_ID,"<%s,%d>\n",string,id);
									id++;
								}
                         }
                        else
                         {
                         		while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                         }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         	}
                         }
                         else
                         {
                         while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                         }
                    }
                  else
                  {
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                  }
                  n=0;
            }
            else
            {
				 check=(int)ch;
                  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 check=(int)ch;
                               }
                               fprintf(out,"<ID,%d>",id);
							   fprintf(Sym_ID,"<%s,%d>\n",string,id);
                               id++;
                               n=0;
            }
        }
       else if(isdigit(ch))
       {
              char string[100]="";
			  check=(int)ch;
			  while(!(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' '))
                               {
                                 string[n++]=ch;
                                 ch=fgetc(fp);
                                 if(isalpha(ch))
                                  {
									  alpha_check=1;
								  }
                                 check=(int)ch;
                               }
                               if(alpha_check)
                               {
							       	   fprintf(out,"%s",string);
								   fprintf(Sym_ERROR,"%s\n",string);
						       }
						       else
						       {
								   fprintf(out,"<CONSTANT,%d>",constant);
								   fprintf(Sym_CON,"<%s,%d>\n",string,constant);
								   constant++;
							   }
                                n=0;
       }
       else
       {
		 check=(int)ch;
       	 if(ch==EOF || (check >= 33 && check<=45) || check==47 || (check>=58 && check<=63) || (check>=123 && check<=125) || check==10 || ch==' ')
       	  {

			  if(check==10 || ch==EOF)
			   {
				   fprintf(out,"\n");
			   }
			   else if(ch==' ')
			   {
				   fprintf(out," ");
			   }
			  else
			  {
			  fprintf(out,"<%c>",ch);
			  fprintf(Sym_OP,"<%c>\n",ch);
		      }
		  }
		 else
		  {
			  fprintf(out,"%c",ch);
			  //fprintf(Sym_OP,"%c\n",ch);
		  }
		  ch=fgetc(fp);
       }

    }
    fclose(Sym_OP);
    fclose(Sym_ERROR);
    fclose(Sym_CON);
    fclose(Sym_KEY);
    fclose(Sym_ID);
	fclose(out);
    fclose(fp);
}
